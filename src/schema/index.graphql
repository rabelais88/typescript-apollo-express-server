# import * from "index.graphql"
# import * from "user.graphql"

type Query {
  posts(currentCursor: ID!): Posts
  post(id: ID!): Post
  stacks: [Stack]
  index: Index
}

type Posts {
  posts: [Post]
  current: ID!
  hasNext: Boolean
}

# type Mutation {
#   createDraft(title: String!, content: String!, authorEmail: String!): Post!
#   deletePost(id: ID!): Post
#   publish(id: ID!): Post
# }